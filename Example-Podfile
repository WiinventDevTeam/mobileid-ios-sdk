platform :ios, '13.0'

target 'MyApp' do
  use_frameworks!

  # MobileID iOS SDK (binary). Cài trực tiếp từ Git bằng tag phiên bản.
  pod 'MobileIdSdk', :git => 'https://github.com/WiinventDevTeam/mobileid-ios-sdk.git', :tag => '1.0.1'
end
